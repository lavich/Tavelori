## 1. Доступность и построение упражнения

- [ ] 1.1 В `src/domain/learning.ts` добавить `wordExerciseOptions(word, pool, hasVoice)`: для узнавания, сборки, написания, аудирования и понимания на слух — доступен или причина (нет звука, один слог, мало вариантов). Перевести `objectiveExercise` на эти же условия. Проверка — `tests/learning.test.ts`: каждая причина и все доступные у слова без навыков; `tests/plan-golden.test.ts` и `tests/plan.test.ts` зелёные без изменений.
- [ ] 1.2 Добавить `buildWordExercise(word, type, pool, random, hasVoice)`: упражнение заданного вида без учёта навыков либо `null` для недоступного. Проверка — тест: написание и понимание на слух строятся у нового слова; сборка даёт слоги; узнавание — четыре варианта с верным; для слова из одного слога сборка — `null`.

## 2. Экран упражнения

- [ ] 2.1 Создать `src/features/words/WordExerciseScreen.tsx` по решению 2 из design.md: `SessionItem` в памяти, `onAnswer` без записи, «Ещё раз» с новыми вариантами, возврат к слову с `replace`, `onSkip` в аудировании — к слову, подпись «Без учёта прогресса». Недоступный вид, неизвестный тип и отсутствующее слово — сообщение и «К слову». Проверка — `tests/word-exercise.test.tsx`: написание с ответом без ударения даёт «Почти» и правильный вариант; «Ещё раз» сбрасывает ответ; после ошибки нет ступени проще; недоступная сборка по адресу.
- [ ] 2.2 Зарегистрировать `/words/:id/exercise/:type` в `src/app/App.tsx` и сделать путь погружённым, как `/session`: без нижней навигации, с тем же отступом уведомлений. Проверка — тест из 2.1 через маршрутизатор; e2e из 4.1 не видит нижней навигации.
- [ ] 2.3 Тест изоляции (решение 3): на базе с установленным уроком и активным занятием пройти каждый из пяти видов с верным и неверным ответом. Все таблицы `TABLES`, кроме `assets`, совпадают до и после, в `meta` нет `sync:dirty`, `syncEvents` не выдал `changed`, активное занятие не изменилось. Проверка — этот тест зелёный.

## 3. Блок на экране слова

- [ ] 3.1 Добавить хук `useWordExercises(word)` в `src/shared/store.ts` и блок «Упражнения» в `WordScreen` по решению 4: пять строк, «Пройти» или выключенная кнопка с причиной, подпись «Без учёта прогресса», «Потренировать слово» без изменений. Блок только у неудалённого слова, на `SharedWordScreen` его нет. Проверка — тест: у слова курса пять видов, без голоса и файла аудирование и понимание выключены с причиной, у удалённого слова и на карточке по ссылке блока нет.

## 4. Сквозные проверки

- [ ] 4.1 Добавить e2e `tests/e2e/word-exercise.spec.ts`: после установки урока 3.4 открыть `/words/w34-03`, выбрать «Написание», ввести `η κατσαρολα` и увидеть «Почти». Затем «Ещё раз», верный ответ, возврат к слову. После этого в IndexedDB нет событий и состояния карточки для `w34-03`, а `sync:dirty` такой же, как до упражнения. Тот же сценарий в Telegram-эмуляции — «Назад» ведёт на экран слова.
- [ ] 4.2 Прогнать `npm test`, `npm run typecheck`, `npm run lint`, `npm run spec`, `npm run format:check` и `npm run test:e2e` — всё зелёное.
