## 1. Спецификация

- [x] 1.1 Ввести требование о подсказке длины ответа и уточнить в «Типы упражнений», что число символов не считается подсказкой, раскрывающей ответ; проверить `npm run spec`.

## 2. Разбор ожидаемого написания

- [x] 2.1 В `src/domain/syllables.ts` добавить чистую функцию разбора ожидаемого написания в маску: деление на группы по `\s+`, в каждой группе символ помечен как скрытая буква (`\p{L}`, `\p{N}`) или как показываемый знак; функция также отдаёт число скрытых букв. Проверка — расширить `tests/syllables.test.ts`.
- [x] 2.2 Покрыть в том же тесте случаи из спеки: «σπίτι» — одна группа из пяти букв; «το σπίτι» — две группы, 2 и 5, всего 7; «Πώς σε λένε;» — три группы, «;» показан знаком и в счёт не входит; написание с апострофом и с дефисом; конечная сигма скрывается как буква.

## 3. Маска в упражнениях

- [x] 3.1 Добавить в `src/features/learning/session.module.css` стиль маски: ячейка буквы с `border-bottom` по образцу `.gap`, отступ между группами, перенос по группам (`flex-wrap`). Проверка — маска длинной фразы не выходит за край в e2e-прогоне.
- [x] 3.2 Добавить общий внутренний компонент маски в `src/features/learning/exercises.tsx`: видимая часть с `aria-hidden`, рядом `span.sr-only` с текстом «Ответ из N букв». Проверка — тест на то, что диктору доступно число, а не строка подчёркиваний.
- [x] 3.3 Показать маску в `Spelling` первой строкой дока над полем ввода, по `expected` (слово и фраза). Проверка — тест на состав маски у слова с артиклем и у фразы.
- [x] 3.4 Показать маску в `ClozeExercise` над полем ввода, по `cloze.answer`; в ветке с четырьмя кнопками-вариантами маску не показывать. Проверка — тесты на обе ветки, включая карточку, у которой `acceptedAnswers` содержит вариант другой длины.

## 4. Проверки

- [x] 4.1 Написать `tests/answer-length-hint.test.tsx`: маска в написании слова, слова с артиклем и фразы; маска в пропуске с полем ввода; отсутствие маски в пропуске с кнопками и в сборке; неизменность маски при наборе; исчезновение маски после сохранённого ответа; отсутствие второго события ответа.
- [x] 4.2 Проверить, что до ответа маска не раскрывает ответ: в DOM задания нет ни ожидаемого написания, ни его букв — расширить существующую проверку скрытого текста в тестах пропуска и написания.
- [x] 4.3 Прогнать `npm test`, `npm run typecheck` и `npm run spec` — всё зелёное.
- [x] 4.4 Прогнать `npm run test:e2e` и убедиться, что ожидания по доку и полю ввода в `tests/e2e/mixed.spec.ts` остались верны.
