## 1. Спецификация

- [x] 1.1 Ввести требование о подсказке длины ответа и уточнить в «Типы упражнений», что число символов, разметка и первые буквы слов не считаются подсказкой, раскрывающей ответ; проверить `npm run spec`.

## 2. Разбор ожидаемого написания

- [x] 2.1 В `src/domain/syllables.ts` добавить чистую функцию разбора ожидаемого написания в маску: деление на группы по `\s+`, символ помечен как скрытая буква (`\p{L}`, `\p{N}`) или как показываемый знак; скрытый символ своей буквы не хранит; функция отдаёт число букв. Проверка — расширить `tests/syllables.test.ts`.
- [x] 2.2 Покрыть в том же тесте случаи из спеки: «σπίτι» — одна группа из пяти букв; «το σπίτι» — две группы, 2 и 5, всего 7; «Πώς σε λένε;» — три группы, «;» показан знаком и в счёт не входит; написание с апострофом и с дефисом; конечная сигма скрывается как буква.
- [x] 2.3 Открывать первую букву каждого слова (`lead`) — и артикля, и самого слова; у ответа из одной буквы не открывать. Проверка — тесты на «σπίτι», «το σπίτι», «Πώς σε λένε;» и на ответ из одной буквы.
- [x] 2.4 Добавить `agreedMask`: общая маска допустимых ответов только при совпадении структуры, иначе `null`; открытая буква берётся из канонического написания. Проверка — тесты на «τηλεόραση»/«την τηλεόραση», «Γράφω»/«γράφω» и на пустой список.

## 3. Маска в поле ответа

- [x] 3.1 Добавить в `src/features/learning/session.module.css` стили: поле-подложка `.field`, скрытые текст и каретка поля (`.masked`), ячейки букв с `border-bottom`, открытая буква, набранная буква, перенос по словам (`flex-wrap`). Проверка — маска длинной фразы не выходит за край в e2e-прогоне.
- [x] 3.2 Добавить компонент маски в `src/features/learning/exercises.tsx`: видимая часть с `aria-hidden`, рядом `span.sr-only` с числом слов, числом букв и открытыми буквами. Проверка — тест на то, что диктору доступны числа и буквы, а не строка подчёркиваний.
- [x] 3.3 Показать маску в `Spelling` внутри поля ввода, по `expected` (слово и фраза). Проверка — тесты на слово, слово с артиклем и фразу.
- [x] 3.4 Показать маску в `ClozeExercise` внутри поля ввода, по `agreedMask`; в ветке с четырьмя кнопками-вариантами маску не показывать. Проверка — тесты на обе ветки и на карточку с допустимыми ответами разной структуры.

## 4. Набор прямо в маске

- [x] 4.1 Раскладывать слова набора по группам маски: пробел переводит к следующему слову, лишние буквы показываются за ячейками своего слова, маска показывает ровно значение поля. Проверка — тесты на «το σπ», на «ηγάτα» без пробела и на ответ без артикля.
- [x] 4.2 Рисовать свою каретку: у правого края последней введённой буквы, в ещё не начатом слове — у левого края первой ячейки; подчёркивания на месте пробела не показывать, пробел не подставлять. Проверка — тесты на пустое поле, середину слова, конец слова, после пробела и на набор длиннее маски.

## 5. Проверки

- [x] 5.1 Написать `tests/answer-length-hint.test.tsx`: маска в написании слова, слова с артиклем и фразы; маска в пропуске с полем ввода; отсутствие маски в пропуске с кнопками и в сборке; заполнение маски при наборе; исчезновение маски после сохранённого ответа; отсутствие второго события ответа.
- [x] 5.2 Проверить, что до ответа маска не раскрывает ответ: в DOM задания нет ожидаемого написания, кроме первых букв слов — расширить существующую проверку скрытого текста в тестах пропуска и написания.
- [x] 5.3 Прогнать `npm test`, `npm run typecheck` и `npm run spec` — всё зелёное.
- [x] 5.4 Прогнать `npm run test:e2e` и убедиться, что ожидания по доку и полю ввода остались верны; добавить проверку, что маска видна и помещается в 360px вместе с полем и кнопкой.
