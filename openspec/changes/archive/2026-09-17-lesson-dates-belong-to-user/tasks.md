## 1. Контракт поставки

- [x] 1.1 Убрать `status` и `targetDate` из `CatalogEntry` и `ContentPackage.lesson` в `src/content/schema.ts`, снять их разбор и поднять `SCHEMA_VERSION` до 2.
- [x] 1.2 Убрать эти поля из сборки каталога и пакета в `content/build.ts`; отклонять их в исходнике урока с объяснением, куда переехал статус.
- [x] 1.3 Убрать `status` и `targetDate` из `content/lessons/*.yaml`; проверить, что публикация проходит и версии пакетов пересчитались.

## 2. Установка и экраны

- [x] 2.1 Заводить установленный урок предстоящим и без собственной даты в `src/content/client.ts`; убедиться, что обновление пакета по-прежнему не трогает статус и дату установленного урока.
- [x] 2.2 Убрать показ статуса и даты неустановленного урока из каталога на экране «Уроки».

## 3. Проверки

- [x] 3.1 Обновить тесты контента и доставки: пакет несёт только название урока, каталог — только метаданные, установка даёт предстоящий урок без даты.
- [x] 3.2 Обновить тесты хранилища и браузерные сценарии, которые опирались на поставляемые статус и дату: расклад задаётся расписанием курса или отметкой урока.
- [x] 3.3 Прогнать `npm run content`, `npm test`, `npm run typecheck`, `npm run spec`, `npm run test:e2e`.
