## 1. План и совместимость

- [x] 1.1 Исключить recall из генерации, добавить объективный fallback; проверить тестами маленький словарь и выбор навыка.
- [x] 1.2 Сохранять отдельное знакомство и преобразовывать старые активные сессии; проверить перезагрузку и сохранность истории.

## 2. Ответы и интерфейс

- [x] 2.1 Удалить самооценку, добавить «Не знаю» во все задания; проверить браузерными тестами раскрытие ответа и отсутствие оценок.
- [x] 2.2 Назначать оценки автоматически и атомарно добавлять ограниченную тренировку ошибок; проверить идемпотентность, режим practice и конфликт версий.

## 3. Проверка

- [x] 3.1 Обновить регрессионные тесты и выполнить npm test, npm run build, npm run test:e2e; записать результат.
- [x] 3.2 Проверить OpenSpec командой openspec validate remove-learning-self-assessment --strict и подготовить коммиты в отдельном worktree.

## 4. Правки после проверки пользователем

- [x] 4.1 Ограничить ширину занятия 560 px на всех экранах; проверено отсутствие переопределения на 620 px.
- [x] 4.2 Добавить существующую кнопку озвучки к выбору перевода; проверка TypeScript и сборка успешны.
- [x] 4.3 Разделить результат и контекст отступом в сборке и написании; в заданиях с вариантами заменить карточку результата цветом, галочкой и крестиком; браузерный тест проверяет цвета, значки и отсутствие отдельного блока.

## Validation

- `npm test`: 111 тестов пройдено.
- `npm run build`: сборка и проверка TypeScript успешны; остаётся предупреждение Vite о размере основного JS-файла.
- `PLAYWRIGHT_PORT=4188 npm run test:e2e`: 18 браузерных тестов пройдено на production build; отдельный порт изолирует проверку worktree.
- `openspec validate remove-learning-self-assessment --strict`: успешно.
- `git diff --check`: успешно.

Проверены все четыре варианта «Не знаю», правильный ответ, сохранение знакомства после перезагрузки, продолжение старой сессии, отсутствие бесконечных дополнительных попыток, откат транзакции и конфликты версий. Исторические события recall сохраняются.
