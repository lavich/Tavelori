## 1. Сервис и реквизиты

- [x] 1.1 Создать проект браузерного JavaScript в Sentry на плане Developer, записать адрес приёма; проверить, что тестовое событие из консоли браузера появляется в проекте.
- [x] 1.2 Добавить в репозиторий переменную `VITE_SENTRY_DSN` и секреты `SENTRY_AUTH_TOKEN`, `SENTRY_ORG`, `SENTRY_PROJECT`; проверить `gh variable list` и `gh secret list`.

## 2. Настройка и границы данных

- [x] 2.1 Добавить в `Settings` поле `errorReports` со значением `true` по умолчанию и подстановкой для старых записей; проверить unit‑тестом, что настройки без поля читаются как включённые, а сохранённое `false` переживает перечитывание.
- [x] 2.2 Написать чистые функции очистки события и фильтра хлебных крошек по границам спецификации; покрыть unit‑тестами: адрес режется до пути, поле пользователя удаляется, дополнительные поля проходят только из разрешённого списка, крошки консоли и кликов отбрасываются, навигация и жизненный цикл остаются.
- [x] 2.3 Проверить аудитом все `new Error(...)` и сообщения ошибок в `src`, что ни одно не включает слово, перевод или ответ пользователя; добавить тест на сообщение ошибки сохранения ответа.

## 3. Модуль отчётов

- [x] 3.1 Реализовать модуль отчётов с `reportError`, ленивой загрузкой SDK отдельным чанком после первого рендера, буфером до десяти ранних ошибок, офлайн‑транспортом, метками `env`, `app.version`, `app.build`, `tg.version`, `tg.platform` и без интеграций трассировки и записи сессий; проверить unit‑тестом, что без адреса приёма функция — пустая операция и динамический импорт не вызывается.
- [x] 3.2 Подключить проверку настройки: инициализация только при включённых отчётах, выключение закрывает SDK и очищает очередь; проверить unit‑тестом оба переключения.
- [x] 3.3 Писать крошки жизненного цикла Mini App (`activated`, `deactivated`, `viewportChanged`, `visibilitychange`) с высотами области просмотра в адаптере платформы; проверить в тестах платформы, что крошки не содержат `initData` и данных пользователя.

## 4. Ловля сбоев

- [x] 4.1 Добавить границу ошибок в корень дерева React и экран сбоя с «Перезапустить» и «Скопировать диагностику»; проверить в jsdom, что компонент, бросающий исключение при рендере, даёт экран сбоя, а диагностика содержит версию и среду без учебных данных.
- [x] 4.2 Вызывать `reportError` с категориями при отказе открытия базы, отклонении пакета урока, ошибке синхронизации и отказе регистрации service worker; проверить unit‑тестами категорию и отсутствие содержимого пакета в дополнительных полях.
- [x] 4.3 Показывать понятное сообщение в корневом элементе при отказе открытия базы вместо пустой страницы; проверить e2e со сломанным IndexedDB, что сообщение видно.
- [x] 4.4 Проверить e2e, что после падения экрана занятия и перезапуска «Сегодня» предлагает продолжить занятие.

## 5. Экраны

- [x] 5.1 Добавить переключатель «Отправлять отчёты об ошибках» на экран «Настройки»; проверить e2e, что выключение переживает перезагрузку страницы.
- [x] 5.2 Описать канал отчётов на экране «Копия данных» рядом с синхронизацией: куда уходят, что содержат, чего не содержат, про офлайн‑очередь и переключатель; проверить e2e наличие описания.

## 6. Сборка и публикация

- [x] 6.1 Включить скрытые карты кода и плагин загрузки, подключаемый только при наличии токена, с удалением `*.map` из `dist` после загрузки и именем релиза «версия+коммит»; проверить, что `npm run build` без токена проходит и в `dist` нет файлов карт.
- [x] 6.2 Передать в workflow публикации переменную адреса приёма и секреты загрузки, оставив проверку pull request без них; проверить зелёный CI на PR и появление релиза с картами в Sentry после публикации.
- [x] 6.3 Зафиксировать в проверке, что размер стартового чанка не вырос, а SDK лежит в отдельном чанке; проверить по отчёту сборки.

## 7. Документация и проверка

- [x] 7.1 Описать канал отчётов и его границы в README и в `docs/telegram.md` (матрица проверок: сбой внутри Telegram доходит до сервиса с метками клиента); проверить, что описание совпадает со спецификацией.
- [x] 7.2 Прогнать `npm run typecheck`, `npm test`, `npm run spec`, `npm run build`, `npm run test:e2e`; проверить, что все зелёные и ни один e2e не делает запросов к сервису.
