## 0. Подготовка и проверка рисков

- [ ] 0.1 Подготовить доступ к основному `@TaveloriBot` и созданному `@TaveloriDevBot`, два реальных устройства с Telegram (iOS и Android) и постоянный HTTPS-адрес первой версии; результат — заполненные реквизиты без секретов в инструкции. По умолчанию остаётся текущий GitHub Pages; перенос на Vercel не выполняется автоматически.
- [ ] 0.2 До основной доработки экранов собрать минимальный прототип на `@TaveloriDevBot`: обмен тестовым снимком между двумя устройствами одного аккаунта, повторное открытие, закрытие во время публикации и независимые офлайн-изменения; результат — отчёт с версиями клиентов, полученными данными и отсутствием молчаливой потери версии. Не использовать реальные пользовательские данные.
- [ ] 0.3 В том же прототипе проверить греческую клавиатуру и доступность кнопки ответа, тестовый аудиофайл/отсутствие голоса, сохранение и импорт файла на обоих устройствах; записать рабочие способы и ограничения. Непроверенные пункты не считать пройденными; исправить блокирующие дефекты до основной адаптации UI. Независимые локальные работы могут продолжаться без устройств.
- [x] 0.4 Зафиксировать таблицу полей компактного снимка и соответствующих потребителей в планировщике/статистике; проверить на эталонной последовательности наличие всех данных для FSRS, двух чистых сборок перед письмом, дневного бюджета и статистики за 7 дней. Оценить размер снимка текущего каталога и растущего набора с резервом на две версии и конфликт, записать фактические числа и границу вместимости.

- [ ] 0.5 В прототипе сравнить MainButton на «Сегодня» и компактную шапку с текущим UI: проверить обе мобильные платформы, нижнюю навигацию, «Начать/Продолжить», loading/disabled, отсутствие дубликатов и доступ к «Ещё». Записать решение и снимки экранов; при непрохождении критериев оставить обычную главную кнопку Lexi.

- [ ] 0.6 Подготовить инструкцию локальной разработки через Vite и HTTPS-туннель для `@TaveloriDevBot`: указать команды запуска, настройку URL и конкретного allowedHosts, способы инспекции iOS/Android и последствия смены origin. Проверить открытие локального приложения по `https://t.me/TaveloriDevBot?startapp` и независимость CloudStorage от основного бота; токен в клиент не добавлять.

## 1. Интеграция платформы

- [x] 1.1 Добавить адаптер официального Telegram bridge и неблокирующую инициализацию в `src/main.tsx`; проверить запуск с bridge, без него, с ошибкой загрузки и с частично доступным API.
- [x] 1.2 Добавить вызовы готовности/расширения и очистку подписок; проверить, что повторное подключение в StrictMode не удваивает обработчики и не создаёт ошибок.
- [x] 1.3 Добавить сообщение о границах облачного и локального прогресса на первом запуске Telegram и экране копий; проверить, что обычный браузер не требует Telegram и приложение не запрашивает контакты, сообщения или аккаунт.

- [x] 1.4 Выделить платформенные back/haptic/primaryAction отдельно от транспорта синхронизации; проверить web fallback, недоступный API, замену обработчиков и очистку при смене маршрута без дублирования в StrictMode.

## 2. Оболочка и тренировка

- [x] 2.1 Объединить обработку внутреннего возврата и Telegram BackButton; проверить возврат из слова и занятия, скрытие на главном экране и переход на главный экран при отсутствии внутренней истории.
- [x] 2.2 Адаптировать высоту, safe-area и док занятия к Telegram и клавиатуре; проверить ширины 360/390 пикселей, изменения viewport и отсутствие перекрытия поля/кнопки ответа в браузерном тесте.
- [x] 2.3 Подключить смену темы к семантическим цветам оболочки; проверить светлую и тёмную темы, контраст текста/статусов и fallback при отсутствии параметров темы.
- [x] 2.4 Обработать скрытие/возврат приложения без переноса записи ответов на закрытие; проверить продолжение сохранённой сессии после перезагрузки и ровно одно событие на принятый ответ, включая повторное нажатие и ошибку записи.

- [x] 2.5 Сопоставить токены оболочки с Telegram CSS-переменными и резервными значениями, сохранив семантические цвета ответов; проверить светлую, тёмную и неполную пользовательскую тему, текст/иконки статусов, контраст и отсутствие сброса упражнения при смене темы.
- [x] 2.6 Добавить отклик результата после локального сохранения и локальный выключатель в Telegram-настройках; проверить правильный/почти правильный/неправильный ответ, отсутствие отклика при пропуске, ошибке записи, повторной обработке и синхронизации, web no-op и исключение API.
- [ ] 2.7 Реализовать выбранный в 0.5 вариант главного действия и компактной шапки: проверить заголовки, доступ к «Ещё», fallback back, отсутствие двойного CTA/двойной сессии и снятие нативного действия при уходе; все кнопки упражнения остаются внутри Lexi.

## 3. Аудио, копии и офлайн

- [x] 3.1 Исправить обработку отказа воспроизведения в `src/shared/audio.ts` и упражнении: повтор или продолжение без аудирования, без штрафа; проверить отсутствие голоса, отказ файла и остановку звука при уходе/скрытии.
- [x] 3.2 Добавить передачу полной копии через поддерживаемый файловый share или скачивание с нейтральным статусом; проверить доступность API, отмену, ошибку и совместимость переданного файла с текущим восстановлением.
- [x] 3.3 Сохранить проверку и подтверждение восстановления, остановить замену при отмене/ошибке передачи защитной копии; проверить перенос текущей и старой копий, повреждённый файл и неизменность базы при отказе.
- [x] 3.4 Обработать недоступность service worker/storage API и уточнить офлайн-статусы оболочки и уроков; проверить работу онлайн без этих API, тренировку скачанного урока при потере сети и понятный отказ для неустановленного урока.

## 4. CloudStorage и профили

- [x] 4.0 Выделить интерфейс адаптера синхронизации с возможностями транспорта, чтением/публикацией версий и типизированными ошибками; реализовать CloudStorage и тестовый транспорт, а для веба отключённый режим. Общий набор контрактных тестов проверяет повторы, частичную запись, несовместимый формат и лимит; доменные модули не импортируют Telegram API. Внешнюю базу и серверный адаптер сейчас не реализовывать.

- [x] 4.1 Добавить изоляцию Telegram-профилей, метаданные синхронизации и очередь с совместимой миграцией; проверить смену аккаунта, отсутствие контекста, перенос старой локальной базы и чтение старых копий без восстановления чужого идентификатора устройства.
- [x] 4.2 Реализовать компактный снимок стандартного прогресса и сводок; проверить эквивалентность сроков FSRS, выбора упражнений, дневного бюджета и статистики за 7 дней на втором устройстве, отсутствие двойного учёта и сохранность неизвестных идентификаторов до загрузки пакета.
- [x] 4.3 Реализовать публикацию поколений по частям с указателем устройства, checksum и причинными версиями; проверить обрыв на каждой стадии, повтор отправки, неполное чтение, неизвестный формат и сериализацию двух вкладок.
- [x] 4.4 Добавить автоматический обмен и ручной повтор со статусом последнего подтверждения; проверить передачу после открытия/возврата/офлайн и отсутствие ложного успеха при ошибке CloudStorage.
- [x] 4.5 Реализовать обнаружение независимых ветвей и выбор целой версии с сохранением альтернатив; проверить одновременную запись двух устройств, позднюю ветвь после разрешения и конфликт после восстановления копии.
- [x] 4.6 Добавить контроль 1024 ключей и 4096 символов, безопасную очистку собственных поколений и экспорт альтернатив; проверить границы вместимости с резервом на запись, запрет удаления чужих/конфликтных данных и сохранение локальной очереди при переполнении.

- [x] 4.7 Проверить разделение веба и Telegram в общем origin: ответы каждого режима не меняют другой профиль, веб не вызывает CloudStorage и не показывает Telegram Login; ручной перенос копии не включает последующую синхронизацию между режимами, а экран хранения показывает правильную область данных.

## 5. Автоматическая проверка и документация

- [x] 5.1 Добавить интеграционные Playwright-сценарии с эмуляцией Telegram для запуска, навигации, темы, viewport, продолжения занятия и копий; проверить прохождение вместе с существующими браузерными сценариями.
- [x] 5.2 Выполнить `npm test`, `npm run test:e2e`, `BASE_PATH=/lexi/ npm run build`; проверить отсутствие регрессии PWA и корректные пути оболочки/контента в сборке, отсутствие токена бота в исходниках интеграции и её настройках сборки.
- [x] 5.3 Подготовить инструкцию настройки Main Mini App в BotFather, прямой ссылки и отката, описать границы облачной синхронизации, конфликтов, копий и офлайн; проверить наличие инструкции и отсутствие секретов, создать таблицу реальных устройств с первоначальным статусом «не проверено».

- [x] 5.4 Подготовить инструкцию возможной смены origin: экспорт на старом адресе, импорт в чистом профиле нового адреса, отдельное восстановление Telegram из того же бота и контроль локальных данных вне облака; проверить сценарий на двух тестовых origin и отметить, что один редирект не переносит IndexedDB. Фактический переезд не входит в задачу.

## 6. Приёмка в Telegram и выпуск

- [ ] 6.1 Настроить Main Mini App `@TaveloriDevBot` на тестовую HTTPS-сборку; проверить открытие экрана «Сегодня» из профиля и прямой ссылки, записать фактическую ссылку в инструкцию.
- [ ] 6.2 Выполнить матрицу на реальном iOS: запуск, скачивание урока, занятие с клавиатурой, темы, возврат, повторное открытие, потеря сети, аудио и перенос копий в обе стороны, перенос компактного прогресса с Android на iOS и обратно под одним аккаунтом, конфликт после офлайн-тренировок и изоляция другого аккаунта; записать версии ОС/Telegram, результаты и найденные ограничения, исправить блокирующие дефекты.
- [ ] 6.3 Выполнить ту же матрицу на реальном Android и записать версии/результаты; не отмечать проверку завершённой при отсутствии устройства или рабочего переноса копии.
- [ ] 6.4 После успешной приёмки подключить `@TaveloriBot` к проверенной сборке; проверить запуск из профиля и `https://t.me/TaveloriBot?startapp`, а также обычный веб-доступ, зафиксировать версию выпуска и способ отката.
