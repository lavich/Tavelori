## 1. Спецификация

- [ ] 1.1 Изменить требования «Расписание занятий» и «Завершение прошедших уроков» в `lesson-library` и «Дневная подготовка» в `adaptive-learning`; проверить `npm run spec`.

## 2. Домен

- [ ] 2.1 Добавить `lessonHour: number` в `Schedule`, значение 12 в `defaultSchedule` и нормализацию при чтении (не целое, вне 0–23 или поля нет — 12). Проверка — тест на расписание без поля и с мусорным значением.
- [ ] 2.2 Добавить `localHour(date, timezone)` рядом с `localDay` в `domain/learning.ts`; проверка — тест на Asia/Nicosia и Europe/Moscow около границы часа.
- [ ] 2.3 Добавить `preparedThrough(now, timezone, lessonHour)` в `domain/schedule.ts`; проверка — таблица: до часа, ровно час, после часа, полночь, смена суток в зоне.

## 3. План

- [ ] 3.1 В `makePlan` заменить сравнения с `today` на сравнения с рубежом курса в разделении `past`/`upcoming`; убедиться, что `daysBetween`, `requiredPerDay` и накопительный счёт остались на календарных днях.
- [ ] 3.2 Проверить `tests/plan-golden.test.ts`: его `now` — ровно 12:00 в Asia/Nicosia, поэтому эталон обязан остаться прежним (ближайший урок там датирован 16 сентября). Фикстуру перегенерировать только если расхождение объяснено.
- [ ] 3.3 Тесты `tests/plan.test.ts`: до часа очередь ведёт сегодняшний урок; после часа — следующий; ровно час считается прошедшим; у двух курсов с разными часами рубежи независимы; дневной бюджет после смены ближайшего занятия не выдаётся заново.

## 4. Закрепление

- [ ] 4.1 В `settleLessons` перейти на рубеж курса вместо `targetDate < today`; повторный вызов по-прежнему ничего не пишет.
- [ ] 4.2 В `App.tsx` заменить наблюдаемое значение `today` на пару «день + локальный час».
- [ ] 4.3 Тесты `tests/storage.test.ts`: урок закрепляется в свой час и не закрепляется за час до; повторный вызов возвращает 0.

## 5. Интерфейс

- [ ] 5.1 В `ScheduleCard` добавить `Select` с 24 значениями «00:00 … 23:00» и подписью о том, что после этого часа Lexi переходит к следующему набору.
- [ ] 5.2 Исправить сохранение в `ScheduleCard`: расписание собирается из полей формы и сейчас затрёт час; сохранять все поля. Проверка — тест на то, что смена дней недели не сбрасывает час.
- [ ] 5.3 Передать курсы в `nextLessonIds` из `TodayScreen` (через `useCourses`) и `LessonsScreen`; подпись «К <дню>» переходит на следующее занятие после часа.

## 6. Перенос данных

- [ ] 6.1 Проверить круг снимка синхронизации: час уходит и возвращается, снимок без часа читается как 12, формат снимка не меняется.
- [ ] 6.2 Проверить полную копию: час сохраняется и восстанавливается, копия без часа принимается.

## 7. Проверки

- [ ] 7.1 Прогнать `npm test`, `npm run typecheck`, `npm run lint` и `npm run spec`.
- [ ] 7.2 Прогнать `npm run test:e2e`; при необходимости поправить сценарии, завязанные на дату занятия.
- [ ] 7.3 Ручная проверка на дев-боте: занятие сегодня, час 12 — до полудня очередь ведёт текущий урок, после полудня следующий.
