# Задачи

## 1. Проигрыватель

- [x] 1.1 Тесты `tests/audio.test.ts` с поддельным `speechSynthesis`: потерянная реплика → повтор → успех; молчание дважды → `error`; ошибка синтезатора → повтор; `cancel()` не вызывается на свободном синтезаторе; пустой список голосов → ожидание `voiceschanged` → успех; таймаут ожидания голосов → `none`
- [x] 1.2 `speak()` возвращает промис и подтверждает старт событием; повтор после отказа; отмена отделена от запуска
- [x] 1.3 `greekVoice()` дожидается списка голосов; синхронные `audioKind`/`textAudioKind` не меняют поведение подписей

## 2. Настройка

- [x] 2.1 `Settings.autoSpeak` со значением по умолчанию `true` и тест на `fillSettings` для старой записи
- [x] 2.2 Переключатель «Озвучивать автоматически» на экране настроек рядом с отчётами и тактильным откликом

## 3. Автозапуск

- [x] 3.1 `Introduction` озвучивает слово и фразу один раз при открытии, если автозапуск включён; пропуск не озвучивается сам, отказ не показывается
- [x] 3.2 `Listening` подчиняется той же настройке
- [x] 3.3 e2e: знакомство звучит без нажатия при включённой настройке и молчит при выключенной; кнопка работает в обоих случаях

## 4. Проверка

- [x] 4.1 `npm test`, `npm run typecheck`, `npm run build`, `npm run test:e2e`
- [ ] 4.2 Проверка на дев-боте в Telegram: знакомство звучит само, «Что прозвучало» перестало молчать
