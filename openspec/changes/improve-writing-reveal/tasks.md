## 1. Спецификация

- [ ] 1.1 Ввести требование о раскрытии материала после письменного ответа и ограничить запрет автозапуска временем до ответа; проверить `npm run spec`.

## 2. Раскрытие

- [ ] 2.1 В `Spelling` убрать задание с экрана после сохранённого ответа и показать `WordReveal`/`PhraseReveal` со `speak`; убрать строку `Правильно: …` из плашки, а также частные случаи `ExampleBox` и `SpeakText`. Проверка — новый тест на состав раскрытия.
- [ ] 2.2 В `Spelling` заменить сообщение при «Не знаю» (`Правильный ответ: ${expected}`) на текст без написания — «Ничего страшного — вот как это пишется.»; посимвольный разбор и легенда остаются только при ошибке и «Почти», как сейчас. Проверка — тест на то, что после «Не знаю» написание в плашке не встречается, а в раскрытии есть.
- [ ] 2.3 В `Assembly` сделать то же, сохранив в плашке строку со слогами; проверка — тест на то, что видны и «η · οι-κο-γέ-νεια», и карточка слова.

## 3. Автозапуск

- [ ] 3.1 Добавить хук автозапуска по появлению неуспешного результата (ошибка, «Почти», «Не знаю») с флагом-ref и сбросом по `item.id`; проверка — тест на ровно один вызов озвучки.
- [ ] 3.2 Проверить сброс флага между карточками: отрисовать `Spelling` без `key`, ошибиться на карточке A — один вызов озвучки, передать карточку B тем же компонентом, ошибиться — ещё один вызов для B. В приложении `SessionScreen` монтирует упражнение заново (`key={item.id}`), тест держит сброс на месте и на случай, если `key` уйдёт.
- [ ] 3.3 Пробросить `autoSpeak` из `SessionScreen` в `Spelling` и `Assembly`; проверка — тест на тишину при выключенной настройке и при верном ответе.

## 4. Проверки

- [ ] 4.1 Написать `tests/writing-reveal.test.tsx` по образцу `tests/listening-reveal.test.tsx`: раскрытие после ошибки, «Почти», «Не знаю» и верного ответа в написании слова, написании фразы и сборке; состав плашки в каждом из четырёх исходов; отсутствие второго события ответа; задание не задваивается.
- [ ] 4.2 Прогнать `npm test`, `npm run typecheck` и `npm run spec` — всё зелёное.
- [ ] 4.3 Прогнать `npm run test:e2e` и убедиться, что ожидания `tests/e2e/assembly.spec.ts` и `tests/e2e/mixed.spec.ts` по плашке остались верны.
