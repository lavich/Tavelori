# Задачи

## 1. Домен

- [x] 1.1 Выделить построение сборки из `objectiveExercise` в общий помощник
- [x] 1.2 Добавить `easierExercise(card, type, pools, random)`: лестница `spelling → assembly → recognition`, `null` вне лестницы и при нехватке слогов или вариантов
- [x] 1.3 Тесты домена: написание → сборка, односложное написание → узнавание, сборка → узнавание, узнавание/аудирование/пропуск → `null`, нехватка вариантов → `null`

## 2. Запись ответа

- [x] 2.1 В `recordAnswer` строить дополнительную попытку через `easierExercise`, читая пул вариантов только при неверном ответе и только для заданий с лестницей
- [x] 2.2 Тесты хранилища: тип и варианты попытки после ошибки в написании и в сборке; попытка после ошибки в узнавании остаётся тем же заданием; правила «одна попытка на карточку» и `mode: 'practice'` не изменились

## 3. Проверка

- [x] 3.1 `npm test`, `npm run typecheck`, `npm run build`
- [x] 3.2 E2E: сценарий ошибки в написании показывает сборку в дополнительной попытке
