## 1. Модель и сборка контента

- [x] 1.1 Добавить в `Example` (`src/domain/types.ts`) необязательное поле `glosses: { start, length, russian, wordId? }[]` и проверить, что `npm run typecheck` проходит
- [x] 1.2 В `content/build.ts` принять `examples[].words` исходника: найти отрезки по порядку в NFC-предложении, вычислить `start`/`length`, отклонить отсутствующий отрезок, пересечение, нарушение порядка и пустой перевод с сообщением, где названы файл, пример и отрезок; проверить тестами в `tests/content.test.ts` на каждый отказ и на успешную сборку
- [x] 1.3 Проверять ссылку `word` по всем словам каталога после сборки всех уроков, отклонять неизвестный идентификатор; проверить тестом со ссылкой на слово другого урока и с неизвестной ссылкой
- [x] 1.4 Убедиться тестом, что добавление разметки меняет ревизию слова (`revisionOf`), а пример без разметки даёт прежнюю ревизию, то есть пакеты без разметки не переустанавливаются

## 2. Чтение пакета и хранение

- [x] 2.1 В чтении пакета (`parsePackage` в `src/content/schema.ts`, через него читает `src/content/client.ts`) проверять форму `glosses`: отрезок в пределах предложения, порядок, непустой перевод, иначе `ContentError("schema")`; проверить тестом в `tests/content.test.ts`
- [x] 2.2 Проверить тестом, что разметка переживает установку пакета, полную копию и восстановление, а копия и снимок без разметки читаются как прежде

## 3. Показ в примере

- [x] 3.1 Переделать `ExampleBox` (`src/features/words/WordCardView.tsx`): размеченные отрезки — кнопки с пунктирным подчёркиванием, выделение слова-цели сохраняется, строка «отрезок — перевод» под предложением с `aria-live="polite"`, повторное нажатие скрывает строку; проверить компонентным тестом смену и скрытие строки и отсутствие кнопок у примера без разметки
- [x] 3.2 Добавить режимы со ссылкой и без (проп `linkFrom` — карточка, в которой показан пример; без него — только перевод): со ссылкой при нажатии на отрезок со ссылкой читать одно слово по ключу и показывать «Открыть карточку» (ссылка `/words/:id`), если слово есть и не удалено и это не текущая карточка; проверить тестами все три случая (есть, нет или удалено, та же карточка)
- [x] 3.3 Подключить `linkFrom` в `WordScreen.tsx`, режим без ссылки в знакомстве и раскрытии в `exercises.tsx`; проверить тестом, что нажатие в тренировке не создаёт событий ответа и не меняет состояние сессии

## 4. Редактор слова

- [x] 4.1 В `WordEditorScreen.tsx` снимать `glosses` у примера при смене его греческого текста (`editExample`), и сохранять их при правке только русского перевода; проверить тестом оба случая

## 5. Инструкция, навык и пилот

- [x] 5.1 Описать разметку в `docs/lesson-authoring.md` (что размечать, ссылки на существующие карточки, запрет новых карточек ради разметки, пример преподавателя только по просьбе, происхождение переводов) и добавить её в шаблон `docs/lesson-authoring/word.yaml`; проверить, что шаблон проходит `tests/authoring.test.ts`
- [x] 5.2 Добавить шаг разметки в `.agents/skills/prepare-lesson/SKILL.md` и проверить, что навык ссылается на раздел инструкции
- [x] 5.3 Пилот: разметить примеры 15 новых слов урока 4.1 (`w41-01`…`w41-15`), указав в `source` слова, что переводы слов примера подготовлены Lexi; проверить `npm run content`

## 6. Проверка целиком

- [x] 6.1 Добавить e2e-сценарий: открыть карточку слова урока 4.1, нажать размеченное слово, увидеть перевод, открыть связанную карточку; проверить на ширине телефона
- [x] 6.2 Прогнать `npm run content`, `npm run typecheck`, `npm test`, e2e и `npx openspec validate add-example-word-glosses --strict`
