## 1. Тематические и предметные роли

- [x] 1.1 Добавить в `content/art/palette.json` поле `themeable` с пятью подложками, `ink` и `blue`; в тесте контента проверить, что каждое имя из списка есть в палитре и что список совпадает с пометками «Тема» в таблице `docs/art-standard.md`.
- [x] 1.2 Обновить `docs/art-standard.md`: колонка «Тема» в таблицах палитры, абзац о двух классах ролей, роль `blue` как акцента и правило «вода, небо, стекло — `blue-soft`», строка об этом в таблице замен раздела «Миграция»; проверить прохождение теста сверки документа с палитрой.

## 2. Библиотека общих частей

- [x] 2.1 В `readSources` (`content/build.ts`) пропускать записи-директории внутри `art/` и `audio/`; проверить тестом, что подпапка `art/parts/` не роняет публикацию и не попадает в медиа.
- [x] 2.2 Создать `content/art/parts/` с семью эталонами `person`, `face`, `house`, `tree`, `sun`, `table`, `cloud`, извлечёнными из существующих картинок и приведёнными к палитре; убедиться, что каждый проходит `checkArt` без legacy.
- [x] 2.3 В тесте контента прогонять все файлы `art/parts/*.svg` через `checkArt` с пустым списком legacy и выводить их первой строкой обзорного листа `docs/art-sheet.html` с подписью имени; проверить, что лист собирается и содержит семь карточек частей.
- [x] 2.4 Добавить в `docs/art-standard.md` раздел «Общие части» со списком эталонов, их назначением и правилом «новая повторяющаяся фигура сначала добавляется в библиотеку».

## 3. Имена файлов и медиа по файлу

- [x] 3.1 В `checkArt` или `mediaFor` отклонять имя файла иллюстрации не по образцу `^[a-z0-9]+(-[a-z0-9]+)*\.svg$` с указанием требуемого вида; тест на греческое имя, заглавную букву и подчёркивание.
- [x] 3.2 Отклонять при публикации два файла иллюстраций с одинаковыми байтами, называя оба имени; тест с двумя одинаковыми файлами.
- [x] 3.3 Образовывать идентификатор медиа иллюстрации от имени файла (`img-<имя>`), собирать медиа в карту по идентификатору, дедуплицировать медиа пакета, считать `art.files` и `art.legacy` по файлам, а не по словам; тест: два слова с общим файлом в одном уроке дают один `imageAssetId` и одну запись медиа, файл учтён один раз в отчёте.
- [x] 3.4 Сделать `alt` медиа иллюстрации пустой строкой в публикации и допустить пустой `alt` в схеме пакета, если он там ограничен; тест схемы и снимок пакета.
- [x] 3.5 Составить таблицу «греческое имя → английское kebab-case» для всех 189 файлов `content/art/*.svg` (все файлы на кого-то ссылаются, копий по байтам нет; одинаковые значения разведены: `money`/`cash`, `friend-man`/`friend-woman`, `light`/`light-weight`); проверить уникальность имён и соответствие образцу.
- [x] 3.6 Одноразовым скриптом вне репозитория переименовать файлы, обновить `image:` во всех `content/words/*.yaml` и строки `content/art/legacy.txt`; проверить, что `npm run content` проходит и печатает счётчики по файлам.
- [x] 3.7 Обновить ссылки на имена файлов в тестах (`το-σπίτι.svg` и другие), в README (пример `content/art/…`) и в ожидаемых числах отчёта о миграции; выполнить тесты контента.

## 4. Тема курса

- [x] 4.1 Читать необязательное `theme:` из файла курса и `content/art/themes/<имя>.json`; отклонять несуществующую тему, роль вне `themeable`, предметную роль и цвет не вида `#rrggbb`; тесты на каждый отказ с именем темы и ролью.
- [x] 4.2 Разворачивать тему в карту «hex палитры → hex темы» и записывать в `CatalogCourse.palette` только для курсов с темой; добавить поле в разбор каталога (`src/content/schema.ts`) как необязательное; тест: каталог без поля читается, каталог с полем даёт карту.
- [x] 4.3 Добавить `palette?` в `Course` (`src/domain/types.ts`), копировать его в `syncCatalog` и учитывать в условии обновления записи курса; тест синхронизации с курсом с темой и без.
- [x] 4.4 Реализовать подмену цветов в `useAssetUrl` для SVG-ассета при непустой карте: замена полных hex с границами, новый blob того же типа, ассет в IndexedDB не меняется; юнит-тест на SVG с `url(#g)`, `href="#g"`, цветами палитры и проверка, что сохранённый ассет не изменился.
- [x] 4.5 В `WordArt` получать курс через первый урок слова и палитру курса из таблицы `courses`, передавать в `useAssetUrl`; браузерный тест: слово курса с темой показывает перекрашенный SVG, слово курса без темы — исходный.
- [x] 4.6 Добавить фикстурный курс с темой в тесты публикации и клиента; убедиться, что курс LEEKE без темы публикуется без `palette` и приложение ведёт себя как прежде.
- [x] 4.7 Описать тему курса в `docs/art-standard.md` (что тема вправе менять, минимальные требования к контрасту подложек и `ink`) и в README (поле `theme:` курса, папка `content/art/themes/`).

## 5. Обзорный лист для ревью

- [x] 5.1 Вынести сборку карточек листа в функцию, принимающую список файлов и карту цветов; подписывать карточку по-английски — имя файла без расширения, русские переводы ссылающихся слов только в `title` карточки; собирать полный лист `docs/art-sheet.html` и проверить тестом, что подписи содержат только латиницу и имя файла.
- [x] 5.2 Собирать `docs/art-changes.html`: читать `git diff --name-status -M <ART_BASE|main> -- content/art` и неотслеживаемые файлы, для изменённых и переименованных брать прежнюю версию через `git show`, ставить рядом; при ошибке git пропускать лист с заметкой; тест на временном репозитории с добавленным, изменённым и переименованным файлом и на папке без git.
- [x] 5.3 Добавить на оба листа переключатель темы: карты тем из `content/art/themes/*.json` через функцию публикации, разметка на тему через клиентскую функцию подмены в `<template>`, `<select>` для выбора; без тем переключатель не выводится; тест с фикстурной темой проверяет, что предметные цвета в разметке темы не тронуты, а тематические заменены.
- [x] 5.4 Описать оба листа и переключатель в `docs/art-standard.md` (раздел «Проверка публикации») и в README.

## 6. Проверка

- [x] 6.1 Выполнить typecheck (`tsc --noEmit --noUnusedLocals`), unit- и браузерные тесты, e2e на видимость картинки, production build и `openspec validate adopt-illustration-system --strict`; всё проходит.
