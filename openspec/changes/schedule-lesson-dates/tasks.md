## 1. Модель и вычисление дат

- [x] 1.1 Добавить в `Settings` поле `schedule` со значением по умолчанию и дополнение отсутствующего поля в `loadSnapshot` и при восстановлении копии; проверить, что копия и запись настроек без расписания читаются с незаданным расписанием.
- [x] 1.2 Создать чистый модуль `src/domain/schedule.ts`: `lessonOrder`, `nextLessonDay`, `scheduleLessons`; проверить порядок по номерам («1.9» < «1.10» < «2.1», без номера — после по дате создания), старт курсора с первого дня не раньше даты первого занятия, сдвиг хвоста после ручной даты, монотонность курсора при ручной дате «назад», пропуск проведённых без даты, поведение при незаданном расписании и совпадение исходного порядка массива.
- [x] 1.3 Подключить `scheduleLessons` в `loadSnapshot`; проверить на fake-indexeddb сценарий из спеки: 1.2 с датой 18 сентября, расписание Пн/Чт с 18 сентября даёт 1.3 → 21 сентября, 1.4 → 24 сентября, а `makePlan` считает сроки по этим датам.

## 2. Операции над уроками

- [x] 2.1 Заменить `saveLesson` на `updateLesson(id,patch)` с чтением сырой записи; убрать дату из `createLesson` и `commitImport`; проверить, что правка названия урока по расписанию не записывает дату в базу.
- [x] 2.2 Реализовать `settleLessons(now)` и вызывать её после `ensureSeed` при запуске и при смене календарного дня; проверить закрепление прошедшего урока, идемпотентность повторного вызова, независимость закреплённой даты от смены дней недели и закрепление ручной даты в прошлом со статусом «предстоит».
- [x] 2.3 Реализовать отметку проведённым с закреплением текущей даты и «Вернуть в расписание» с очисткой даты; проверить, что отметка до даты не сдвигает следующие уроки.

## 3. Экраны

- [x] 3.1 Добавить на экран «Уроки» карточку расписания со свёрнутым описанием и формой: дата первого занятия с ограничением «не раньше сегодня», переключатели дней недели с `aria-pressed`, сохранение, отмена, отключение расписания и сообщения об ошибках; пометить в списке предстоящие уроки с ручной датой.
- [x] 3.2 Перевести экран «Урок» на урок из снимка; добавить подсказку про дату по расписанию и кнопку «Вернуть в расписание»; убрать поле даты из формы создания урока и из импорта, обновить подписи.
- [x] 3.3 Обновить браузерные проверки, которые задавали дату при создании и импорте; добавить сценарий: задать расписание, увидеть даты 1.3 и 1.4, перенести 1.3 вручную и увидеть сдвиг 1.4, вернуть 1.3 в расписание.

## 4. Приёмка

- [ ] 4.1 Обновить README: раздел «Что уже есть» описывает расписание и закрепление прошедших уроков.
- [ ] 4.2 Выполнить typecheck, unit-тесты, production build и браузерные проверки; убедиться, что экран «Сегодня» показывает ближайший срок по дате из расписания.
