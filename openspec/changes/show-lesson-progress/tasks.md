## 1. Данные

- [x] 1.1 Добавить в домен статистики чистую группировку слов урока на устойчивые, в повторении и новые; проверить unit-тестом границы: `Review` с интервалом 21 день — устойчивое, 20 дней — в повторении, `Learning` и `Relearning` — в повторении, без состояния — новое.
- [x] 1.2 Заменить в `lessonViews` поле `newCount` на `progress` с тремя группами, считая их из уже прочитанных состояний по живым словам; проверить unit-тестом на базе, что сумма групп равна `wordCount`, удалённое слово не учитывается, а без флага прогресс не читается.
- [x] 1.3 Дополнить эталон масштаба: список 200 уроков читает не больше одного состояния на связь и, как раньше, ни одной карточки и ни одной записи истории.

## 2. Компонент строки

- [x] 2.1 Создать `LessonRow` с иконкой, заголовком, описанием, полосой из трёх сегментов и текстом прогресса; полоса с ролью `img` и подписью, сегменты с подписями групп, у пустого урока полосы нет.
- [x] 2.2 Подключить `LessonRow` на «Сегодня» и «Уроках» вместо дублирующей разметки; обоим экранам отдавать список с прогрессом.

## 3. Проверка

- [x] 3.1 Добавить браузерный сценарий: у урока 1.1 на «Сегодня» и «Уроках» видно «33 новых»; после засеянных состояний четырёх слов — «4 в повторении · 29 новых»; после занятия числа урока 1.2 меняются.
- [x] 3.2 Выполнить typecheck, unit- и браузерные тесты, production build и проверку спек.
