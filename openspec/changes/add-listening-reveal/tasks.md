# Задачи

## 1. Общая карточка

- [x] 1.1 Выделить `WordReveal({word, speak})` из тела `Introduction` и превратить `PhraseIntro` в `PhraseReveal({phrase, speak})`; флаг `speak` включает кнопку озвучки
- [x] 1.2 `Introduction` использует обе с `speak` — внешний вид знакомства не меняется

## 2. Аудирование

- [x] 2.1 Тест: после выбора в аудировании слова в разметке есть перевод, IPA и пример; после верного ответа — то же, что после ошибки и «Не знаю»
- [x] 2.2 `Listening` передаёт карточку в `after` у `Choice` без кнопки озвучки
- [x] 2.3 Тест на фразу: перевод, ситуация и примечание; отсутствующие части не ломают показ

## 3. Проверка

- [x] 3.1 e2e `tests/e2e/objective-learning.spec.ts`: проходим аудирование и видим перевод
- [x] 3.2 Убедиться, что баннер отказа аудио (`tests/e2e/audio.spec.ts`) не задет: он в `head`, карточка в `after`
- [x] 3.3 `npm test`, `npm run typecheck`, `npm run spec`, `npm run build`, `npm run test:e2e`
