## 1. Спецификация

- [x] 1.1 Распространить требование о восстановлении после сбоя хранилища на фоновые задачи вне дерева интерфейса.

## 2. Лечение

- [x] 2.1 Вынести политику лечения (предел попыток за окно, переоткрытие базы, крошка с номером попытки) в общий модуль хранилища и взять из него предел для границы Recovery.
- [x] 2.2 Повторять заход обмена после вылеченного отказа хранилища, сообщать о сбое только когда лечение не помогло; накрыть общей обработкой первое чтение базы в заходе.
- [x] 2.3 Подключить лечение к координатору приложения и записывать крошку восстановления.

## 3. Проверки

- [x] 3.1 Тест: закрытая база (IndexedDB после сна) переоткрывается, обмен доходит до `synced`, отчёта нет, крошка с номером попытки есть.
- [x] 3.2 Тест: переоткрытие не удалось — ровно один отчёт и статус `error`.
- [x] 3.3 Тест: лечение ограничено тремя попытками за минуту и не берётся за ошибки не от хранилища.
