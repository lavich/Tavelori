## 1. Платформенный адаптер

- [x] 1.1 Добавить адаптеру активность: `active()` и `onActiveChange`; в Telegram-адаптере подписаться на `activated`/`deactivated` безопасным вызовом и снимать подписку в `dispose`; веб-адаптер всегда активен.
- [x] 1.2 Описать `isActive` в типах bridge.

## 2. Оболочка

- [x] 2.1 Удерживать последнюю положительную высоту в `--app-height`; нулевые и неизвестные значения игнорировать, при уходе с Telegram-адаптера сбрасывать.
- [x] 2.2 В хуке окружения подписаться на активность адаптера и `visibilitychange`; на возврат пересчитать переменные оболочки, состояние активности показать в `data-app-active`.

## 3. Проверка

- [x] 3.1 Unit-тесты адаптера: подписка и снятие `activated`/`deactivated`, чтение `isActive`, клиент, отвергающий неизвестное событие; удержание высоты при нулевом viewport.
- [x] 3.2 Эмулятор bridge: `isActive`, `deactivate()` с нулевым viewport, `activate()`.
- [x] 3.3 Браузерный сценарий «свернули → вернули» во время занятия: экран виден, высота положительная, упражнение то же, тема перечитана; сценарий видимости документа для клиента без Bot API 8.0.

## 4. Документация

- [x] 4.1 `docs/telegram.md`: описание жизненного цикла, строка матрицы о сворачивании и возврате, список автоматических проверок.
- [x] 4.2 `docs/telegram-dev.md`: как проверить сворачивание на устройстве и что смотреть в инспекторе.
- [x] 4.3 Выполнить typecheck, unit- и браузерные тесты, production build и проверку спек.
