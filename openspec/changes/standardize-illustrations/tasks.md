## 1. Палитра и стандарт

- [ ] 1.1 Создать `content/art/palette.json`: холст, потолок размера, подложки и цвета фигур с именами ролей.
- [ ] 1.2 Написать `docs/art-standard.md`: холст, подложка, палитра, композиция, абстрактные слова, размер, запреты, анимация, миграция; упомянуть стандарт в README.

## 2. Проверка публикации

- [ ] 2.1 Разбирать цвета SVG из атрибутов и `style`, нормализовать запись и сверять с палитрой; допускать `none`, `currentColor`, `transparent`, `inherit` и прозрачность атрибутами.
- [ ] 2.2 Отклонять при публикации размер больше 3 КБ, чужой `viewBox`, текст, заголовки, скрипты, стили, анимацию, растр, внешние ссылки и `data:`-адреса.
- [ ] 2.3 Читать `content/art/legacy.txt`, освобождать перечисленные файлы от проверки палитры, отклонять имена без файла и файлы, уже соответствующие палитре; печатать число иллюстраций и файлов вне палитры в `npm run content`.
- [ ] 2.4 Заполнить `legacy.txt` текущими файлами вне палитры и убедиться, что публикация с текущими 189 файлами проходит.

## 3. Тесты

- [ ] 3.1 Проверить отказ публикации на каждое нарушение: размер, `viewBox`, текст, скрипт, стиль, анимация, внешняя ссылка, растр, цвет вне палитры в новом файле.
- [ ] 3.2 Проверить путь миграции: унаследованный файл вне палитры публикуется, имя без файла и файл в палитре отклоняются, потолок числа строк списка.
- [ ] 3.3 Сверить набор цветов в `docs/art-standard.md` с `palette.json`; проверить, что все проверяемые файлы проходят остальные проверки стандарта.
- [ ] 3.4 Дополнить обзорный лист легендой палитры и подсветкой файлов вне палитры.
- [ ] 3.5 Выполнить typecheck, unit- и браузерные тесты, production build и проверку спек.

## 4. Последующая работа (не блокирует это изменение)

- [ ] 4.1 Перерисовать 187 иллюстраций из `legacy.txt` под палитру: сначала подложки, затем файлы с одним-двумя чужими цветами, затем остальные; список сокращается по мере перерисовки.
