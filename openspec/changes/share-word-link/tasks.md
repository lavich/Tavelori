## 1. Индекс слов в каталоге

- [ ] 1.1 В `content/build.ts` добавить в запись урока каталога `wordIds` — идентификаторы слов урока в порядке состава. Проверка — тест сборки в `tests/content.test.ts`: у записи урока есть `wordIds`, совпадающие с `words` пакета, `schemaVersion` остаётся 3.
- [ ] 1.2 В `src/content/schema.ts` добавить необязательное `wordIds?: string[]` в `CatalogEntry` и его разбор в `parseCatalog` с проверкой строк; каталог без поля принимается. Проверка — `tests/schema.test.ts`: каталог с полем, без поля и с нестроковым элементом (отказ с путём поля).
- [ ] 1.3 Прогнать `npm run content` и убедиться, что `catalog.json` содержит `wordIds` у всех уроков и вырос не больше чем на 10 КБ.

## 2. Поиск и чтение пакета без установки

- [ ] 2.1 Вынести из `httpFetcher` функцию `contentUrl(url, base = BASE_URL)` и перевести `httpFetcher` на неё. Проверка — тест: база `/`, `/lexi` и `/lexi/` дают один адрес без `//`; существующие тесты загрузки зелёные.
- [ ] 2.2 Добавить состояние `catalogPhase` (`loading` / `ready` / `error`), которое выставляет `refreshCatalog`: `loading` только до первого `ready`, повторные вызовы и ошибка после `ready` фазу не сбрасывают. Хук `useCatalogPhase` через `useSyncExternalStore`. Проверка — тест: успех, ошибка до первого успеха, повторный вызов после `ready` (фаза не уходит в `loading`), ошибка после `ready` (остаётся `ready`).
- [ ] 2.3 В `refreshCatalog` сохранять у записи `position` — индекс в `catalog.lessons`; в `src/content/client.ts` добавить `lessonOfWord(wordId)`: записи `db.catalog` по `position` (без него — в конец, затем по id), первая с этим словом в `wordIds`, либо `null`. Проверка — тест в `tests/content-delivery.test.ts`: слово из одного урока; слово из `lesson-2-1` и `lesson-10-1`, где `lesson-2-1` раньше в каталоге, — возвращается `lesson-2-1`; отсутствующее слово; каталог без `wordIds`.
- [ ] 2.4 Добавить `previewPackage(lessonId)`: чтение пакета через `fetcher`, проверка `parsePackage`, сверка `pack.id` и `pack.version` с записью каталога, кэш в памяти, никаких записей в базу. Проверка — тест на поддельной базе: после вызова таблицы `lessons`, `words`, `lessonItems`, `packages`, `courses`, `assets` не изменились; ошибка сети превращается в `ContentError` вида `network`; пакет другого id или версии отклоняется.
- [ ] 2.5 Вынести из `mergeWord` чистое преобразование `PackageWord` → `Word` для показа и использовать его в обоих местах. Проверка — существующие тесты установки зелёные и новый тест преобразования.

## 3. Источник медиа

- [ ] 3.1 Добавить контекст `AssetSource` (`url(assetId): Promise<string | null>`) в `src/shared/store.ts`; `useAssetUrl` читает его, источник по умолчанию — через `ensureAsset`, как сейчас. Проверка — существующие тесты карточки зелёные без изменений.
- [ ] 3.2 Расширить `playWord(word, source?)` в `src/shared/audio.ts`: файл берётся из переданного источника, по умолчанию — из базы; `SpeakButton` передаёт источник из контекста. Проверка — `tests/audio.test.ts` зелёные без изменений и новый тест: с источником просмотра играет URL пакета, `ensureAsset` не вызывается, запасной голос не включается.
- [ ] 3.3 Добавить источник просмотра: по id медиа из `pack.media` отдаёт `contentUrl(url)` без записи в `db.assets`. Проверка — тест: `WordArt` в этом контексте рисует `img` с URL пакета, `db.assets` пуста.

## 4. Экран просмотра и маршрут

- [ ] 4.1 Создать `src/features/words/SharedWordScreen.tsx` по решению 5 из design.md: переход на `/words/:id` для установленного неудалённого слова, просмотр для удалённого, загрузка, пока `catalogPhase` — `loading` (без таймаута), карточка только для просмотра без тренировки и редактирования с некликабельной подписью «Слово из урока <название>», «Слово не найдено», сбой загрузки каталога или пакета с «Повторить». Проверка — `tests/shared-word.test.tsx` на каждое состояние, в том числе переход из загрузки к карточке, когда каталог приходит позже монтажа; повторное обновление каталога не возвращает карточку в загрузку; на карточке нет ссылки на `/lessons/…` и нажатие на подпись ничего не пишет в базу.
- [ ] 4.2 Зарегистрировать `/share/word/:id` в `src/app/App.tsx`. Проверка — тест из 4.1 монтирует экран через маршрутизатор.

## 5. Контекст запуска

- [ ] 5.1 В `src/platform/launch.ts` определять бота так: `?bot=` из адреса, иначе бот сохранённого контекста, иначе `TaveloriBot`; сохранять в `lexi:launch` контекст и для веба (только вид и бот). Проверка — `tests/platform.test.ts`: Telegram с `?bot=TaveloriDevBot` → перезагрузка без `?bot=` сохраняет `TaveloriDevBot` и имя базы профиля; веб с `?bot=` → адрес без параметра сохраняет бота; явный `?bot=` в новом адресе заменяет сохранённый.
- [ ] 5.2 Добавить в `LaunchContext` поле `launchId` (`query_id` из `tgWebAppData`; без него и вне Telegram — `null`) и сохранять его вместе с контекстом. Проверка — тест: разбор `query_id`, `null` без него (даже при наличии `hash`), восстановление из `sessionStorage` сохраняет `launchId`.
- [ ] 5.3 Добавить `startRoute(launch)`: `w_<id>` → `/share/word/<id>`, прочее → `null`. Проверка — тест: `w_w34-03`, `promo`, `w_`, `w_a/b`, id длиннее 60 символов, запуск без Telegram.
- [ ] 5.4 В `App` переходить по `startRoute` с `replace`. При `launchId` — только если он не равен `lexi:start-consumed` в `sessionStorage`, после перехода записывать туда `launchId`. Без `launchId` или при недоступном `sessionStorage` — один раз за время жизни страницы по флагу в памяти, метку не читать и не писать. Проверка — тест: первый монтаж переходит; повторный монтаж с тем же `launchId` — нет; новый `launchId` с другим или тем же `start_param` при старой метке — переходит; без `launchId` повторный монтаж в той же странице не переходит, после сброса модуля (перезагрузки) переходит, и метка в `sessionStorage` не появляется.

## 6. Кнопка «Поделиться»

- [ ] 6.1 Добавить в `src/platform` функцию `shareWord(word, bot)`: в Telegram — `openTelegramLink` на `t.me/share/url` со ссылкой `https://t.me/<бот>?startapp=w_<id>` и текстом «<греческий> — <перевод>»; в браузере — `navigator.share`, иначе копирование и `toast.success("Ссылка скопирована")`; отмена пользователем (`AbortError`) не копирует. Дописать `openTelegramLink` в `telegram-types.ts`. Проверка — тест на три ветки с подменёнными API.
- [ ] 6.2 Добавить реактивный хук `useWordLesson(id)` в `src/shared/store.ts` на `useLiveQuery` над `db.catalog` с логикой `lessonOfWord`. Показать кнопку «Поделиться» в правой части шапки `WordScreen` рядом с «Редактировать», только если хук вернул урок и у слова пуст `deletedAt`; подпись для диктора «Поделиться словом». Проверка — тест: слово курса с кнопкой; своё слово и удалённое слово курса без неё; кнопка появляется без перемонтажа после записи каталога с `wordIds`.
- [ ] 6.3 Проверить бота ссылки: в браузере без `?bot=` — `TaveloriBot`, с `?bot=TaveloriDevBot` — dev-бот, в том числе после перехода и перезагрузки. Проверка — тест `shareWord` вместе с `launchContext` на эти случаи.

## 7. Сквозные проверки

- [ ] 7.1 Добавить e2e `tests/e2e/share.spec.ts`: на чистой базе `/share/word/w34-03` показывает «η κατσαρόλα», иллюстрацию и подпись урока без ссылки; после этого в IndexedDB нет уроков, слов, подписок и медиа; `/share/word/w99-99` показывает «Слово не найдено».
- [ ] 7.2 Дополнить `tests/e2e/telegram.spec.ts`:
  - dev-запуск с `?bot=TaveloriDevBot` и `start_param=w_w34-03` открывает слово;
  - после перезагрузки «Поделиться» даёт ссылку на `TaveloriDevBot`, а база профиля прежняя;
  - с `query_id` перезагрузка остаётся на текущем экране;
  - новый запуск в той же вкладке (новый `query_id`, `start_param=w_w12-01`) открывает `w12-01`.
- [ ] 7.3 Прогнать `npm test`, `npm run typecheck`, `npm run lint`, `npm run spec` и `npm run test:e2e` — всё зелёное.
- [ ] 7.4 Вручную на `TaveloriDevBot`:
  - в BotFather адрес Mini App содержит `?bot=TaveloriDevBot`;
  - ссылка из «Поделиться» ведёт на `TaveloriDevBot` и открывает Mini App на этом слове у второго аккаунта без установленного урока;
  - записать в design.md, приходит ли `query_id` при запуске по `startapp`.
