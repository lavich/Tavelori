## 1. Индекс слов в каталоге

- [ ] 1.1 В `content/build.ts` добавить в запись урока каталога `wordIds` — идентификаторы слов урока в порядке состава. Проверка — тест сборки в `tests/content.test.ts`: у записи урока есть `wordIds`, совпадающие с `words` пакета, `schemaVersion` остаётся 3.
- [ ] 1.2 В `src/content/schema.ts` добавить необязательное `wordIds?: string[]` в `CatalogEntry` и его разбор в `parseCatalog` с проверкой строк; каталог без поля принимается. Проверка — `tests/schema.test.ts`: каталог с полем, без поля и с нестроковым элементом (отказ с путём поля).
- [ ] 1.3 Прогнать `npm run content` и убедиться, что `catalog.json` содержит `wordIds` у всех уроков и вырос не больше чем на 10 КБ.

## 2. Поиск и чтение пакета без установки

- [ ] 2.1 В `src/content/client.ts` добавить `lessonOfWord(wordId)` — первая по порядку каталога запись из `db.catalog`, в `wordIds` которой есть слово, либо `null`. Проверка — тест в `tests/content-delivery.test.ts`: слово из одного урока, из двух уроков, отсутствующее, каталог без `wordIds`.
- [ ] 2.2 Добавить `previewPackage(lessonId)`: чтение пакета через `fetcher`, проверка `parsePackage`, кэш в памяти, никаких записей в базу. Проверка — тест на поддельной базе: после вызова таблицы `lessons`, `words`, `lessonItems`, `packages`, `courses`, `assets` не изменились; ошибка сети превращается в `ContentError` вида `network`.
- [ ] 2.3 Вынести из `mergeWord` чистое преобразование `PackageWord` → `Word` для показа и использовать его в обоих местах. Проверка — существующие тесты установки зелёные и новый тест преобразования.

## 3. Источник медиа

- [ ] 3.1 Добавить контекст `AssetSource` в `src/shared/store.ts`: `useAssetUrl` и воспроизведение звука слова берут ресурс из контекста, по умолчанию — через `ensureAsset`, как сейчас. Проверка — существующие тесты карточки и аудио (`tests/audio.test.ts`) зелёные без изменений.
- [ ] 3.2 Добавить источник просмотра: по id медиа из `pack.media` отдаёт `BASE_URL + url` без записи в `db.assets`. Проверка — тест: `WordArt` в этом контексте рисует `img` с URL пакета, `db.assets` пуста.

## 4. Экран просмотра и маршрут

- [ ] 4.1 Создать `src/features/words/SharedWordScreen.tsx` по решению 5 из design.md: переход на `/words/:id` для установленного слова, ожидание каталога, карточка только для просмотра без тренировки и редактирования с плашкой урока, «Слово не найдено», сбой загрузки с «Повторить». Проверка — `tests/shared-word.test.tsx` на каждое из пяти состояний.
- [ ] 4.2 Зарегистрировать `/share/word/:id` в `src/app/App.tsx`. Проверка — тест из 4.1 монтирует экран через маршрутизатор.

## 5. Параметр запуска Telegram

- [ ] 5.1 В `src/platform/launch.ts` добавить `startRoute(launch)`: `w_<id>` → `/share/word/<id>`, прочее → `null`. Проверка — `tests/platform.test.ts`: `w_w34-03`, `promo`, `w_`, `w_a/b`, id длиннее 60 символов, запуск без Telegram.
- [ ] 5.2 В `App` один раз за запуск переходить по `startRoute` с `replace` и ставить метку `lexi:start-consumed` в `sessionStorage`; при недоступном `sessionStorage` переходить один раз за время жизни страницы. Проверка — тест: первый монтаж переходит, повторный с меткой — нет.

## 6. Кнопка «Поделиться»

- [ ] 6.1 Добавить в `src/platform` функцию `shareWord(word, bot)`: в Telegram — `openTelegramLink` на `t.me/share/url` со ссылкой `https://t.me/<бот>?startapp=w_<id>` и текстом «<греческий> — <перевод>»; в браузере — `navigator.share`, иначе копирование и `toast.success("Ссылка скопирована")`; отмена пользователем (`AbortError`) не копирует. Дописать `openTelegramLink` в `telegram-types.ts`. Проверка — тест на три ветки с подменёнными API.
- [ ] 6.2 Показать кнопку «Поделиться» в правой части шапки `WordScreen` рядом с «Редактировать», только если `lessonOfWord(word.id)` не `null`; подпись для диктора «Поделиться словом». Проверка — тест: слово курса с кнопкой, своё слово без неё.

## 7. Сквозные проверки

- [ ] 7.1 Добавить e2e `tests/e2e/share.spec.ts`: на чистой базе `/share/word/w34-03` показывает «η κατσαρόλα», иллюстрацию и ссылку на урок; после этого в IndexedDB нет уроков, слов, подписок и медиа; `/share/word/w99-99` показывает «Слово не найдено».
- [ ] 7.2 Дополнить `tests/e2e/telegram.spec.ts`: запуск с `start_param=w_w34-03` открывает слово, перезагрузка остаётся на текущем экране.
- [ ] 7.3 Прогнать `npm test`, `npm run typecheck`, `npm run lint`, `npm run spec` и `npm run test:e2e` — всё зелёное.
- [ ] 7.4 Вручную на `TaveloriDevBot`: ссылка из кнопки «Поделиться» открывает Mini App на этом слове у второго аккаунта без установленного урока.
