## 1. Проверка сборки в домене

- [x] 1.1 Восстанавливать в `src/domain/syllables.ts` написание из выложенных плиток, считая первую плитку местом артикля; проверить тестом на «η οικογένεια», «το φρούτο» и слове без артикля, что верная раскладка даёт исходное написание после нормализации Unicode и внешних пробелов.
- [x] 1.2 Добавить проверку сборки, возвращающую `correct`, `almost` или `wrong` вместе с сообщением и восстановленным ответом; проверить тестами: верная раскладка — `correct`, верные слоги с артиклем не на первой позиции — `almost` с сообщением про артикль, неверный порядок слогов — `wrong`, слово без артикля «Почти» не получает.
- [x] 1.3 Развернуть дополнение пула старой сессии: артикль добавляется в начало, если его не хватает, и не дублируется, если он уже есть; проверить тестами на пуле без артикля, пуле с артиклем, слове без артикля и на «το φρούτο» в обоих видах.

## 2. Пул упражнения

- [x] 2.1 Класть в варианты ответа элемента сессии перемешанный пул целиком — артикль и слоги; проверить тестом, что состав пула совпадает с артиклем и слогами, порядок отличается от правильного, а «το φως» сборку по-прежнему не получает.
- [x] 2.2 Поправить комментарий о детерминированности потока `random` в `src/domain/learning.ts` под новое поведение; проверить, что золотые тесты плана проходят без изменений ожиданий.

## 3. Экран сборки

- [x] 3.1 Убрать подпись «Слово дано с артиклем», элемент выложенного артикля и класс `.article` из `src/features/learning/session.module.css`; проверить, что фраза не встречается в `src`, `tests`, `content`, `docs`, `README.md` и `openspec/specs`, а элементов `article-hint` и `fixed-article` в разметке нет.
- [x] 3.2 Перевести экран на доменную проверку и три состояния плашки обратной связи, как в написании от руки, и передавать `status` в сохранение ответа; проверить тестом компонента, что «Почти» показывает своё сообщение и уходит в сохранение ответа статусом.
- [x] 3.3 Открывать проверку только когда выложены все плитки пула, включая артикль, и возвращать поставленный артикль в пул как любую другую плитку; проверить браузерным сценарием.

## 4. Совместимость и приёмка

- [x] 4.1 Обновить браузерный сценарий сборки: подписи и закреплённого артикля нет, артикль ставит пользователь, проверка закрыта до последней плитки, старая сессия без артикля в пуле доигрывается; проверить `npm run test:e2e`.
- [x] 4.2 Прогнать приёмку: `npm run typecheck`, `npm test`, `npm run spec`, `npm run build`, `npm run test:e2e` — все зелёные.

При архивации требование «Сборка слова из плиток» встанет в конец спеки, а не на место прежней «Сборки слова из слогов»: OpenSpec не умеет удалять сценарий внутри MODIFIED, поэтому требование заменяется целиком через REMOVED и ADDED. Порядок требований в спеке — косметика, вручную его не править.
