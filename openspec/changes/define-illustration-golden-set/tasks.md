## 1. Краткий визуальный норматив

- [x] 1.1 Сохранить исходный Golden Set в `docs/references/golden-set-v1.png`; создать корневой `STYLE.md` со ссылкой на него и параметрами холста `320×220`, безопасным полем 20, основной обводкой 7–9, круглыми концами и соединениями, правилами перспективы, детализации, силуэта, сложности и запретами; тестом проверить ключевые числовые значения и отсутствие расхождений с `palette.json`.
- [x] 1.2 Дополнить `docs/art-standard.md` ссылкой на `STYLE.md` и объяснением различия между `parts/` и `references/`; проверить, что тест документации проходит.

## 2. Эталонные предметы

- [x] 2.1 Создать `content/art/references/objects/apple.svg` и `book.svg` по утверждённому визуальному нормативу; проверить оба файла через `checkArt` с пустым legacy и визуально при 64 px.
- [x] 2.2 Создать `content/art/references/objects/house.svg`, `car.svg` и `phone.svg`; проверить три файла через `checkArt` с пустым legacy и визуально при 64 px.

## 3. Эталонные персонажи

- [x] 3.1 Создать `content/art/references/characters/cat.svg` и `dog.svg` с одинаковой системой пропорций лица, корпуса и конечностей; проверить оба файла через `checkArt` и сравнить рядом на обзорном листе.
- [x] 3.2 Создать `content/art/references/characters/child.svg`, `mother.svg` и `person.svg` с общей системой лица и тела; проверить три файла через `checkArt`, правильный `id` корневой группы и читаемость силуэта при 64 px.

## 4. Эталонные действия и сцены

- [x] 4.1 Создать `content/art/references/actions/run.svg` и `sleep.svg`, передав действие позой и минимальным контекстом; проверить оба файла через `checkArt` и визуально без подписи.
- [x] 4.2 Создать `content/art/references/actions/drink.svg`, `read.svg` и `rain.svg`; проверить три файла через `checkArt`, правильный `id` и узнаваемость без текста.

## 5. Проверка и обзорный лист

- [x] 5.1 Добавить тест точного состава трёх каталогов, корневой группы с `id` по имени файла, прохождения всех 15 SVG через `checkArt` без legacy и отсутствия эталонов в опубликованных медиа; выполнить тест контента.
- [x] 5.2 Реализовать сборку `docs/art-references.html` с тремя секциями по пять карточек, основной картинкой и миниатюрой 64 px; добавить файл в `.gitignore` и проверить структуру листа тестом.
- [x] 5.3 Открыть обзорный лист и провести визуальную сверку 15 эталонов по толщине линии, пропорциям, плотности деталей, палитре и силуэту; исправить найденные расхождения и повторно выполнить тест контента.

## 6. Итоговая проверка

- [x] 6.1 Выполнить `tsc --noEmit --noUnusedLocals`, полный набор unit-тестов, production build и `openspec validate define-illustration-golden-set --strict`; все команды должны пройти.
